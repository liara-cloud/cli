export default function help() {
  console.log(`
Usage: liara [options] [command]

Deploy apps with a single command

Options:
  -v, --version  output the version number
  -h, --help     output usage information

Commands:
  deploy  deploy a project
  `);
}
